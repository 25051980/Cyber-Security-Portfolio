[pytest]
addopts = -q
testpaths = tests
markers =
    future: Spec tests for not-yet-implemented features (expected to xfail)
    nfr: Non-functional requirement checks (performance/usability/security)
    security: Security-focused tests

