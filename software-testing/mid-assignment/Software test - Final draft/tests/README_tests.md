
# Test Suite – Online Bookstore

## Prerequisites
- Python 3.9+
- macOS/Linux shell (zsh/bash) or VS Code integrated terminal

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip
python -m pip install -r requirements.txt
